# Try CPU-only TensorFlow for Render
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.5
numpy>=1.21.0,<1.25.0
pillow>=9.0.0
motor>=3.0.0
pymongo>=4.0.0
python-dotenv>=0.19.0
nest-asyncio>=1.5.0
gunicorn>=20.0.0
setuptools>=80.9.0

# CPU-only TensorFlow (smaller, more compatible)
tensorflow-cpu>=2.13.0,<2.16.0
setuptools>=65.0
wheel>=0.37.0
pip>=22.0

# If TensorFlow still fails, try these alternatives:
# onnxruntime>=1.15.0
# scikit-learn>=1.3.0