# Core dependencies based on imports in your files

# TensorFlow and ML (from train_model.py and app.py)
tensorflow==2.13.1
numpy==1.24.3

# Image processing (PIL imports in both files)
pillow==10.0.1

# FastAPI and web server (from app.py)
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# MongoDB async driver (motor imports in both files)
motor==3.3.2
pymongo==4.6.0

# Async support (nest_asyncio import in train_model.py)
nest-asyncio==1.5.8

# System monitoring (psutil import in app.py - optional)
psutil==5.9.6

# CORS middleware (included in fastapi but explicit for clarity)
# fastapi already includes starlette

# Standard library imports that don't need installation:
# - os, json, asyncio, io, time, datetime, logging, collections, base64
# - These are part of Python standard library

# Optional dependencies your code checks for:
# gridfs - included with pymongo
# bson - included with pymongo